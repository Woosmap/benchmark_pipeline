[pytest]
testpaths = tests

# Le projet n'est pas installé : la racine doit être sur sys.path pour que
# `import src.template_question...` résolve.
pythonpath = .

addopts =
    --strict-markers
    -ra

markers =
    slow: test dont la durée dépasse quelques secondes (DuckDB par question)
